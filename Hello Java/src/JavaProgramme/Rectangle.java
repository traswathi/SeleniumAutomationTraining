package JavaProgramme;

/*6.Create a class named 'Rectangle' with two data members 'length' and 'breadth' 
 * and two methods to print the area and perimeter of the rectangle respectively. 
 * Its constructor having parameters for length and breadth is used to initialize length 
 * and breadth of the rectangle. Let class 'Square' inherit the 'Rectangle' class with 
 * its constructor having a parameter for its side (suppose s) calling the constructor of 
 * its parent class as 'super(s,s)'.
 *  Print the area and perimeter of a rectangle and a square.*/

public class Rectangle
{
int length,breadth;

Rectangle()
{
	System.out.println(Rectangle);}
Rectangle(int l,int b)
{
	length=l;
	breadth=b;
	//public void area();
	
	
}

public void area()
{
	System.out.println(Length is :+length);
	System.out.println(Length is :+breadth);
int area=length*breadth;
System.out.println(area);
}

}
